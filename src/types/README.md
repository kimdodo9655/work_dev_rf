# Types

## 📅 수정일

2025-01-21

## 📦 포함 내용

총 25개 타입 파일

```
types/
├── api.ts                    # 공통 API 타입
├── common.ts                 # 공통 유틸리티 타입
├── dialog.ts                 # 다이얼로그 타입
├── env.d.ts                  # 환경변수 타입
├── error.ts                  # 에러 타입
├── index.ts                  # 통합 export
└── domains/
    ├── address.ts            # 주소 관련 타입
    ├── auth.ts               # 인증 관련 타입
    ├── bank.ts               # 금융기관 타입
    ├── branch.ts             # 지점 타입
    ├── code.ts               # 공통코드 타입
    ├── notification.ts       # 알림 타입
    ├── organization.ts       # 기관 타입
    ├── user.ts               # 사용자 타입
    └── registry/             # 등기 관련 타입
        ├── admin-info.ts     # 행정정보 동의
        ├── application.ts    # 등기 신청서
        ├── attachment.ts     # 첨부서면
        ├── certificate.ts    # 권리증/증명서
        ├── contract.ts       # 계약 정보
        ├── estimate.ts       # 등기 견적
        ├── index.ts          # registry 통합 export
        ├── progress.ts       # 등기 진행
        ├── property-tax.ts   # 부동산 & 세금
        ├── receipt.ts        # 접수정보
        └── rpa.ts            # RPA 자동화
```
